# start HETS interactive mode
# send the inputForBlend to HETS along with the command to create blends


def make_blendoid(inputForBlendoid)

if File.exists?(inputForBlendoid)
	print "creating blendoid...!!!\n"	
	return
else
	print "File does not exist...\n"
	abort
end
# open the blendoid dol file, wipe it clean for new base

# start HETS interactive session
# send the input file for creating the blend

# write the blendoid in a file.




end

# MC: is this old code? If then, remove it from the repository
